using System;

namespace ConsoleTest
{
    class Program
    {
        static void Main(string[] args)
        {
            //Values must be read as a string then converted to a numeric type
        }
    }
}
